{% extends 'base.html.twig' %}

{% block title %}Comentarioalbum index{% endblock %}

{% block body %}
    <h1>Comentarioalbum index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Idalbum</th>
                <th>CreadoEl</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for comentarioalbum in comentarioalbums %}
            <tr>
                <td>{{ comentarioalbum.id }}</td>
                <td>{{ comentarioalbum.descripcion }}</td>
                <td>{{ comentarioalbum.idalbum }}</td>
                <td>{{ comentarioalbum.creadoEl ? comentarioalbum.creadoEl|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('comentarioalbum_show', {'id': comentarioalbum.id}) }}">show</a>
                    <a href="{{ path('comentarioalbum_edit', {'id': comentarioalbum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('comentarioalbum_new') }}">Create new</a>
{% endblock %}
