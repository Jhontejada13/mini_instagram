{% extends 'base.html.twig' %}

{% block title %}Comentarioimagen index{% endblock %}

{% block body %}
    <h1>Comentarioimagen index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Idimagen</th>
                <th>CreadoEl</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for comentarioimagen in comentarioimagens %}
            <tr>
                <td>{{ comentarioimagen.id }}</td>
                <td>{{ comentarioimagen.descripcion }}</td>
                <td>{{ comentarioimagen.idimagen }}</td>
                <td>{{ comentarioimagen.creadoEl ? comentarioimagen.creadoEl|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('comentarioimagen_show', {'id': comentarioimagen.id}) }}">show</a>
                    <a href="{{ path('comentarioimagen_edit', {'id': comentarioimagen.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('comentarioimagen_new') }}">Create new</a>
{% endblock %}
